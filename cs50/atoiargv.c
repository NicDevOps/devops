#include <stdio.h>
#include <string.h>
#include <math.h>
#include <cs50.h>
#include <ctype.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
        if (!atoi(argv[1]) >= 1 || !atoi(argv[1]) <= 4)
	{
              printf("Blah");
	}

       return 0;
}